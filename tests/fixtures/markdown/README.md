# Markdown fixtures

Корпус фиксирует deterministic GFM import без обращения к сети.

- `supported.md` — front matter, headings, links, task list, table, code и
  first-party rich fence.
- `unsafe.md` — raw HTML, который обязан остаться inert fallback.
