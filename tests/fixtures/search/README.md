# Search fixtures

`mixed-language.json` — открытый synthetic corpus без пользовательских данных.
Он фиксирует Cyrillic/English morphology и mixed-token baseline для
`bm25-fasttext.v1`. Большая performance projection генерируется
детерминированно и не коммитит 500k private-like chunks.

